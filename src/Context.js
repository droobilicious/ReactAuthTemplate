import { createContext   } from 'react';

/* Create contextxs to use globally */
//export const TestContext = createContext();
export const AppContext = createContext();
export const AuthContext = createContext();

